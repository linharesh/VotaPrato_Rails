<html>
<body>
	<h1>Listagem de Restaurantes</h1>
	<h2>Restaurantes: </h2>

<table>
  <tr>
    <th>ID</th>
    <th>Nome</th>
    <th>Endereço</th>
    <th>Especialidade</th>
  </tr>	

 <% @restaurantes.each do |restaurante| %>
   <tr>
   	<td> <%= restaurante.id %> </td>
   	<td> <%= restaurante.nome %> </td>
    <td> <%= restaurante.endereco %>
    <td> <%= restaurante.especialidade %> </td>
    <td> <%= link_to 'Mostrar', action: 'show', id:restaurante %> </td>
   <!-- <td> <%= link_to 'Deletar', {action: 'destroy', id: restaurante}, {method: "delete"} %> </td> -->
    <td> <%= link_to 'Editar', action: 'edit', id: restaurante %>
   </tr>
   <% end %>
 </table>
  <br>

  <%= link_to 'Novo Restaurante', action: 'new' %>

</body>
</html>